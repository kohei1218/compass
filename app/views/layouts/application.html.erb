<!DOCTYPE html>
<html>
  <head>
    <title>Compass</title>
    <%= csrf_meta_tags %>
    <%= yield :header_tags %>

  </head>

  <% body_classes ||= @body_classes %>

  <body class="<%= body_classes %>">
    <%= content_for?(:content) ? yield(:content) : yield %>
  </body>
</html>
